import { Client, ClientOptions } from '@urql/core';
/** Retrieves a Client from Svelte's context */
export declare const getContextClient: () => Client;
/** Sets a Client on Svelte's context */
export declare const setContextClient: (client: Client) => void;
/** Creates Client and adds it to Svelte's context */
export declare const initContextClient: (args: ClientOptions) => Client;
